#include <iostream>
 

int main()
{

		int varA, varB, varC, varD, varE;
		printf("please insert five numbers: ");
		scanf_s("%d %d %d %d %d", &varA, &varB, &varC, &varD, &varE);
        getchar();

		int varAdded = (varA + varB + varC + varC + varD + varE);
		getchar();
		if (varAdded > 0)
	

			
			getchar();
}